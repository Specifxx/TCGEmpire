// Marker for the synthetic forum accounts created by scripts/seed-forum.ts.
// Every seeded persona has an email ending in this domain, so both the seeder
// and the admin "clear dummy data" tool can recognise (and only ever touch)
// their own generated data — real user posts are never affected.
export const SEED_DOMAIN = "@riftcompare.seed";
